
<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(https://www.journaldugeek.com/content/uploads/2020/04/cinema.jpg);">
  <div class="container">
    <h1><%= @list.name %></h1>
    <br>
    <p  class="movie-tag"><%= pluralize(@list.movies.size, 'movie') %></p>
    <%= link_to "Back to menu", root_path, class: "btn btn-primary" %>
  </div>
</div>



 <h1></h1>
  </div>


<div class="container mb-5">
  <div class="d-flex justify-content-between my-4">
    <span class="mb-2 d-block"><small><em class="text-muted"><%= "You haven't saved any movies yet!" if @list.movies.empty? %></em></small></span>
    <div>
      <%= link_to "Add a movie", new_list_bookmark_path(@list)%>
      <%= link_to "delete this list", list_path(@list), method: :delete, class: "btn btn-cancel", data: { confirm: "Are you sure you want to delete this list?" } %>
    </div>
  </div>
  <div class="row my-2">
    <% @list.movies.each do |movie| %>
      <% bookmark = Bookmark.find_by(list: @list, movie: movie) %>
        <div class="card" style="width: 18rem;">
        <%= image_tag movie.poster_url, alt: movie.title, class: "card-img-top" %>
        <div class="card-body">
        <p class="card-text"> <strong><%= movie.title %> <span class="text-muted font-weight-normal">- <%= movie.rating %>/10</span></strong></p>
      </div>
    </div>


    <% end %>
  </div>
  <hr class="my-5">

</div>
